spring:
  application:
    name: mars-backend
  config:
    import: optional:file:.env[.properties],optional:file:../.env[.properties]
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Europe/Istanbul

  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enable: ${MAIL_STARTTLS_ENABLED:true}
          connectiontimeout: ${MAIL_CONNECTION_TIMEOUT_MS:5000}
          timeout: ${MAIL_TIMEOUT_MS:5000}
          writetimeout: ${MAIL_WRITE_TIMEOUT_MS:5000}

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

mars:
  cors:
    allowed-origins: ${MARS_CORS_ALLOWED_ORIGINS:http://localhost:5173}
  jwt:
    secret: ${MARS_JWT_SECRET:MARS_DEV_JWT_SECRET_KEY_CHANGE_ME_32CHARS_MIN}
    expiration-ms: ${MARS_JWT_EXPIRATION_MS:86400000}
    password-reset-expiration-ms: ${MARS_JWT_PASSWORD_RESET_EXPIRATION_MS:900000}
  frontend:
    base-url: ${MARS_FRONTEND_BASE_URL:http://localhost:5173}
  mail:
    enabled: ${MAIL_ENABLED:true}
    from: ${MAIL_FROM:${MAIL_USERNAME:}}
    test-recipient: ${MAIL_TEST_RECIPIENT:}
    reminder-scan-ms: ${MAIL_REMINDER_SCAN_MS:60000}
    reminder-window-minutes: ${MAIL_REMINDER_WINDOW_MINUTES:2}
  appointment:
    status-update-cron: ${MARS_APPOINTMENT_STATUS_UPDATE_CRON:0 */5 * * * *}
    status-update-tolerance-minutes: ${MARS_APPOINTMENT_STATUS_UPDATE_TOLERANCE_MINUTES:15}
  waitlist:
    offer-duration-minutes: ${MARS_WAITLIST_OFFER_DURATION_MINUTES:60}
  penalty:
    no-show-cron: ${MARS_PENALTY_NO_SHOW_CRON:0 */5 * * * *}
    lift-cron: ${MARS_PENALTY_LIFT_CRON:0 0 * * * *}

    offer-check-cron: ${MARS_WAITLIST_OFFER_CHECK_CRON:0 */1 * * * *}

logging:
  level:
    root: INFO
    com.mars: INFO
